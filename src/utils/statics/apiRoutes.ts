const apiRoutes = {
  login: () => "/api/login",
  classes: {
    getAll: () => "/api/classes",
    post: () => "/api/classes",
  },
  students: {
    multiple: () => "/api/studentsAll",
  },
}

export default apiRoutes
